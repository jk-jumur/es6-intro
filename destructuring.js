const product = {
      name: 'Kia EV3',
      price: 8000000,
      description: 'A smartCar by kia'
};

const newPrice = product.price + 100;
const phoneName = `this is ${product.name}`;
console.log(phoneName);
